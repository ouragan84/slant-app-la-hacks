import React, {useState, useEffect, useRef} from "react";

export default () => {

    return (
        <>  
            <p style={{fontSize:30, fontFamily: 'Open Sans'}}>Anton Petrov</p>
        </>
    )
}